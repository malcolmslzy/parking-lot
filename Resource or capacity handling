/=============================================================================
  MODULE:       Member 3 - Resource or Capacity Handling
  REQUIREMENTS: d. Constraint Handling (i. Limited capacity, ii. Prevent invalid ops)
                b. Data Management Component (iii. Display system state)
                
  OWNER:        [MUHAMMAD ILLHAM BIN MUHAMMAD RIDZUAN YAN]
  MATRIC NO:    [BI25110133]
=============================================================================*/

#include "parking.h"
#include <iostream>

using namespace std;

// Handles Requirement d.i: Limited capacity checking
bool checkCapacity() {
    if (carCount >= MAX_SLOTS) {
        return false; 
    }
    return true; 
}

// Handles Requirement d.ii: Preventing invalid operations (out of bounds slot)
bool isSlotValid(int slotNumber) {
    if (slotNumber < 1 || slotNumber > MAX_SLOTS) {
        cout << "[ERROR] Invalid slot number! Please choose a slot between 1 and " << MAX_SLOTS << ".\n";
        return false; 
    }
    return true;
}

// Handles Requirement b.iii: Displaying current system state
void displayCapacityStatus() {
    int available = MAX_SLOTS - carCount;
    cout << "\n========================================";
    cout << "\n         PARKING CAPACITY REPORT        ";
    cout << "\n========================================";
    cout << "\n Maximum Slot Limit : " << MAX_SLOTS;
    cout << "\n Occupied Slots     : " << carCount;
    cout << "\n Available Slots    : " << available;
    cout << "\n System Status      : " << (checkCapacity() ? "AVAILABLE" : "FULL");
    cout << "\n========================================" << endl;
}
